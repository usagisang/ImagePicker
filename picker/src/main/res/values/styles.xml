<resources>
    <!-- Base application theme. -->
    <style name="PickerTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_picker_primary</item>
        <item name="colorPrimaryDark">@color/color_picker_primary_dark</item>
        <item name="navigationIcon">@drawable/ic_ui_close</item>
    </style>

</resources>